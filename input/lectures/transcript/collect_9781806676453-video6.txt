Now, the ADK provides this really powerful mechanism that allows us to hook into an agent's execution process, something what we call a callback. So the idea is we want to be able to observe and customize and even control the agent's behavior at a certain, at a specific time. So we can sort of create this predefines points without modifying the actual core ete from our code. So essentially what we're saying is there will be times when you want to make sure that you want to add filtering and some sort of guardrails in your multiagent workflow. So imagine you have an agent here and you want something to happen before the agent callback. So whatever. So we want to intercept something, the events or the workflow or the process before the agent is called before the process starts. And also we may want to intercept or look at things or guide our agent or get information after the agent is done doing what it was supposed to do. OK. So we have here the before agent callback and this after agent callback going back to what are these callbacks, what is it callback? In essence, callbacks? Are these standard functions, they are just defined as functions and then you can associate these functions with an agent when you create it. So the A K framework will automatically call your function that you created, that you set up at that stage. So think of it just like this is more of a checkpoint during the agent's execution process. So a before agent callback, right, this will execute right before this main agent here. Main work begins for that specific request. And the after agent callback as the name imply, this will execute right after our agent has finished all its steps for that request. At this point, we have all of the final result, but it's going to be just before the result is actually returned for down the line process in the same fashion for agent, we also have the model callbacks. So we have before callback or before model callback and after model callback and for tools as well, we have before tool callback. And after tool callback for model callback, it's going to be called right before the agent is ready to send the information to the model before it's able to send anything to the model. Now, after model callback, this happens right after the model was actually called and has all that information. So right before that is processed, that's when we make that call after model callback and the same with the tools as well. Now the question here is why do we want to even use these callbacks? Well, the reason being is because these callbacks allow us to sort of unlock significant flexibility that we have and also enables advanced agent capabilities for instance, to observe and debug. Because you may want to debug information at a critical step for monitoring and troubleshooting your agent's workflow. You can also customize and have more control because you are able to modify the flow of data through the agents. For instance, you have large LS requests or tool results. You want to see what's going on and you want to be able to modify the data flow through the process of these agents passing and receiving information to and from models and to other agents. And most importantly, you can imagine that within this whole process, you can implement guard rails, so you can enforce safety rules, you can validate inputs or outputs or prevent this allowed operations. You can imagine you can create a filter system guardrail really where you want to make sure that bad words are not allowed to be sent to the model. OK? And you can create that by using either one of these callbacks, depending on what you're looking at, could be for tools could be for models or directly for the agent. So callbacks have a lot of functionalities including also if you want to trigger external A P I's actions, notifications or add features like caching. If you want to also manage the state, you can imagine at some point in this whole process, you may say, well before agent callback, I wanted to implement some sort of dynamic way of updating the agent session or send data somewhere else or get data from somewhere else. And before that we pass to the agent, so you can see that we have this flexibility that we receive or we gain by using callbacks.

All right. So I have this before agent callback. But before that, we are importing a few things that we will need. But most importantly, you can see that we could use the agent, right, agents from agents or just LLM agent. Either one of those work, I'm going to just use LLM agent for this example. So we have the before agent callback notice. We're passing this callback context object here and we're returning the content types. These are specific gen a Google gen I types that have a lot of pieces such as content and all of that as you can see here, we called before the agent to process the user message. Great for logging authentication and important validation. So we're just printing things out here. So nothing really crazy as you can see starting process calls the callback because it has pieces of information and then we get the user content. OK? If we want, we could also very good. And then we have the time here that we're printing when they started, of course, we store that time into the context, callback context state under start time. And then of course, when that is hit it's just going to go ahead and say none to return. And which means continue processing normally. Now, with the after agent callback here, the same thing, we are passing the callback context, we are passing here the doc string, which is very important for agents called after the agent generates a response. And all of that, and this is where we calculate how long it took and we return sort of the response from the agent. So we are actually getting from the state context state and then process that through to dictionary to look at what we're getting back. So I'm just going to remove this. We don't need that in this case, don't modify the response which is return. Not all right. So I'm going to call this math tutor. You can call whatever you want and add a simple description. Maybe this is not a math tutor, but it's OK. It doesn't matter. We can use our functions here or our tools whenever we want. So we have the instructions and all of this. And importantly, we have this before agent callback. This is where we pass these before agent callback function. And we have this Asian callback function, which we are passing here. All right. So let's go ahead and do a quick run here just to see how this would work. So I'm going to be using ADK web. OK. Let's see. Hello world. OK. So you can see the agent who ran agent ran fine. We have traces. You can click here, you can see the invocation, it ran under those milliseconds to call the LLM and everything. But let's go to events you can see in the events here, you can click same thing parts that are coming in in the world and metadata and all the pieces and the actions and all of that. Let's go to look at something else. Let's go to look at the state. You can see the state says start time. This is when they start at the bottom here. When we run, you can see the response, all the things that happen. So you can see that now it found the root agent all is good and then starting the process. This is where we are seeing the hello world that we added. And then the time is this when that happened, this happened before. If you go back, this was called before under before agent called back, that's why we have these pieces showing printing here after we get the response. So after the callback, after agent callback, once the agent was called and did what he needs to do. This is where we see the agent responded with this starting time. And then we have all this information here. This is showing exactly how these callbacks can be used with agents so that we can inject, we can do things debugging, observe what's going on and do more complex things. This is just a simple example, if you come here, this is how we calculate how long it took. And that's why we get the response like this, which is exactly what we have.

Alright. So now let's look at the model callback here to see how that would work. So the essence is still the same. I'm going to put all the code here. So you can see, so you will see that we are doing some imports here. One thing you'll notice is that we have this LM request and LM response because the basis of everything is that we are looking at the responses from the model before and after and that is what it is going to determine the rejection, the filtering process. And that you can see in our before model callback, we're still passing the callback context. But you can see we have this LM request because that is again what I explained earlier just now. OK? And the optional response is going to be an LLM response type. So here you can see we call, this is called before sending the request to. The model is good for content filtering and all of that. And here we get the user's message from the request. So we probe in and look at the response or user message that has been sent before anything. So we can get that message and do whatever we want to filter and all of that. So you can see here, we just have this printc sending to the model. But before that, we can see this block math question. This is just an example. So you can actually connect to a database or a large file with all the keywords that you want to pick up from the query coming in from the user and then block. OK. So in this case, I just have math calculate plus minus multiply divide, equal plus and so forth. So the idea here is that we're going to use these keywords to block anything that comes in that may have those keywords before model callback. You can see the functionality of these callbacks. OK? So we go through the process, we're going to print blocking math related content if any of these are found in the query. OK? Now, the idea is that we're going to return a custom response instead of calling the model. That's why we have this response object and we're going to create our own response but not sending to the actual model. So we are creating our own that says, hey, sorry, I'm not allowed to help you with math problems today and then we continue. OK. So after model callback here, the same thing, we are passing the LM response and returning an optional list of LM response. So this is called after getting a response from the A I So model response is showing there and we're going to add a funny emoji to every response. So now what we'll do here, we're going to say, OK, things are good is after we get a response and actual response, we're going to just add an emoji as an example and then we return the modified response, right? So we have the root agent. I'm going to use LLM agent could have used agent. That's fine. We have a few specifications here, instructions and importantly, we have this before model callback and after model callback. Ok. Let's go ahead and run using the A K web. Let's go ahead and find a model callback and let's give it a try. Let me just try this one. What's two plus two? Let's see. And Voila sorry, I'm not allowed to help with math problems right now. Try asking about something else. This is good. This is exactly what we want with the events. You can see this is what happened but in state you can see nothing really is showing here because we didn't even go and fetch or touch the model because we filter right away and got responded with this. OK? So this is a real good filtering but I say hello world look what will happen. Sorry, I'm not allowed to help with my problems. Looks like it still stick to that. Let's try something like what is the weather like today? Ok. When you do that, you can see. It does exactly what you would expect. It went ahead and sorry, I do not have access to real time weather information, but it added the emojis because this is exactly what we said, we're going to get the response, of course. And then we said add emojis like this can say KQ late five plus five. OK. I'm not allowed to answer math questions. Please ask me something else. So this is good. This is really good. So it is working. Of course, the filtering, you would want to make this even better. Of course, this is just an example, but you get the gist of how this works. So this is after model callback and this is before model hallback. And the distinction here is that we pass the callback context, but most importantly, we pass the LLM request for each one of those because we are focusing on the information between coming from the models or recreating everything. But also we have this LM response which is what is returned. All right. So I'm not going to do the tool one because it's essentially the same thing. But that's something I want you to do, do some research, go to ADK documentation and look up how to implement the callback for tools.

So one thing we can do, we know that we have to use ADK web, which puts together this very nice user in the face. We can interact with our agents, but we can also say ADK help. So we can see other things, other options we can run with ADK. You can see here, we can go ahead and say version to get the version help. And these are the commands we can run. So we've run, we've run the web and you can also say run which runs an interactive CLI for certain agents. And we have evolve which you can look at and test it out. We also have deploy which deploys an agent to a hosted environment. And we can also use create, it can create, this creates a new app in the current folder with prepopulated information. So this is really nice, especially if you don't want to go back and create manually, each one of these folders as you create different agents. OK? So it's very handy. The other thing you can do here, let's see what else we have API server. So API server starts a fast API server for the agent. Let's go ahead and run this API server real quick. So we can see. So ADK API server, this will serve an actual API which we can play with. So again, command click or control click. And when you first do this, you can see that this says here not found because we actually have to go to docs for instance. And you can see that it shows us these fast API docs which gives us all of the endpoints. We can go to list apps, we should list all the apps. We can also get by passing app name, user ID session to get a session we can post. In this case, creates a session with a certain ID can delete, we can post do all these different things. OK? So this is really, really good. We can also run an agent. So let's go ahead and click here real quick to see what we can do to run an agent. You can see this is the schema that is run internally. Can you go ahead and say try out and let's go ahead and see if we can execute. So we can see that we executed using this cURL. And the response is that it looks like session on phone because we would have to pass the session and all these things in order to run an agent. But in any case, you can see that we can do all these things using an API endpoint, which would be this API endpoint for slash run and the actual agent to run. There we go. So this is really nice for cases when you want to interact with the actual API. Now let's go ahead and run this list apps here real quick. I'm going to open and let's try it out and no parameters needed. I'm going to execute. And you can see that this cURL was launched and the response body comes in, we have agent callback model, callback, multiagent, parallel agent, persistent agent and se state runner. So these are indeed all of the apps that we have created, which incidentally if you go back here, you can see these will be all of the ones that we create. 123456. OK? So 123456.